"""
A function named mean, which takes a list of integers as an argument and returns the arithmetic mean of the values in the list.
"""

def mean(lst : list):
    list = sum(lst)/len(lst)
    return list
